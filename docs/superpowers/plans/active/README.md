# Active Plans

Checked-in execution plans for work that needs more than a lightweight in-thread checklist.

Use `YYYY-MM-DD-short-name.md`. Keep each plan focused on one goal, with acceptance criteria, expected files, steps, and verification. Move finished plans to `../completed/` after the implementation is verified.
